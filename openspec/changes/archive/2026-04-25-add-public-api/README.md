# add-public-api

Phase 4.2 — JSON HTTP API surfacing the corpus and engine output for researchers and other tools. /api/verbs (list), /api/verbs/[lemma] (full table; ?format=json|igt|conllu), /api/openapi.json (machine-readable spec). All endpoints are statically pre-rendered; responses are stable contracts.

# add-search-and-browse

Phase 2.1 — Verb discovery surface. /search route with client-side FlexSearch index (built at compile time from corpus); /browse with class/auxiliary/frequency filters; reverse lookup (type a conjugated form, find the lemma). Introduces the search-and-browse capability.

# add-ipa-stress-marking

Add primary-stress (ˈ) marking to the IPA transcription pipeline. Albanian stress is mostly predictable (penultimate by default with patterned exceptions). Implement a syllabification pass + stress placement algorithm + per-form override mechanism. Modifies pronunciation.

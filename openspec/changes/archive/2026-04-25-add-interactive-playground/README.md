# add-interactive-playground

Phase 3.2 — Interactive playground at /playground. User picks any corpus verb and toggles mood, tense, voice, polarity, modality, person, number; the engine recomputes live and the result renders with role-coded coloring + tooltips. Deep-link via URL search params. Builds on conjugation-engine, decomposition, search-and-browse.

# expand-stress-override-registry

Audit every corpus verb's IPA stress against Newmark/Buchholz reference. Hand-curate data/stress-overrides.json entries where the default rule (penultimate + Class 1 -j-lemma exception) misfires. Adds scripts/audit-stress.ts as a regression gate. Modifies pronunciation.

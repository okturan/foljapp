# add-pronunciation

Phase 5.1 — IPA transcription. Albanian orthography is near-phonemic, so a small phoneme-mapping helper renders any form as IPA. Lemma + principal parts on verb pages get an IPA line. Helper exposed to articles + playground via the engine-adjacent lib.

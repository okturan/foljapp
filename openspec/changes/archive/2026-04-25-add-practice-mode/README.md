# add-practice-mode

Phase 3.3 — Quiz/practice surface at /practice. User starts a session, gets prompted to conjugate a random verb in a random cell, types the answer, gets scored. v1 covers active-voice affirmative-declarative cells across all six moods. Deterministic via ?seed=N for testing.
